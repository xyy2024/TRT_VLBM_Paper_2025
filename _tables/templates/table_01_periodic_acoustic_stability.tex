\begin{table}[htbp]

\centering
\caption{Acoustic results aggregated over all eight grids. The fitted factor $\widehat r$ uses steps $50$--$250$; $n_{\rm end}$ is common to every grid for each method. OTRT completes 800 steps; SRT stops at gain $10^3$. Physical times are $n_{\rm end}/(2N^2)$.}
\label{tab:numerical-periodic-acoustic}
\begingroup
\small
\begin{tabular}{@{}lrrrr@{}}
\toprule
Method & $|z_*|$ & $\max_N|\widehat r-|z_*||$ & $n_{\rm end}$ & $\max_N D_2(n_{\rm end})$\\
\midrule
% CSV_ROWS
\bottomrule
\end{tabular}
\endgroup
\end{table}
